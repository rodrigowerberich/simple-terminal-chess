#pragma once

#include <vector>

#include "BoardPosition.hh"

namespace Chess{
namespace Rules{
namespace KnightMoveRequester{

// The knight has no path. It only skips in a L shape movement
// std::vector<Chess::Board::Position> knightPath(const Chess::Board::Position& initialPosition, const Chess::Board::Position& finallPosition);


}
}
}